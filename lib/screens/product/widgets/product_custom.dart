import 'package:flutter/material.dart';

class ProductCustom extends StatelessWidget {
  const ProductCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Custom');
  }
}
